---
"@radoslavirha/tsed-configuration": patch
---

Document where configuration values come from in the `using-tsed-configuration` skill: the `config/` file layout and the `default.json` → `{NODE_ENV}.json` → environment variable load order, with guidance on which layer a value belongs in.
